<% provide(:title, 'Password reset') %>

<h1>Reset password</h1>

<%= form_tag password_resets_path, method: :post do %>

	<%= label_tag :email %>
	<%= text_field_tag :email, params[:email] %>

	<%= submit_tag 'Send password reset link', class: 'btn btn-large btn-primary' %> 
	
<% end %>